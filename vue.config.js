module.exports = {
    pwa: {
      // configure the workbox plugin
      workboxPluginMode: 'GenerateSW',
  
      manifestOptions: {
        name: "Decks-POC",
        short_name: "Decks-POC",
        background_color: "#12b0db",
    }
  }
}
